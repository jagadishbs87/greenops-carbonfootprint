# GreenOps AI Service

FastAPI service that scores cloud regions based on carbon intensity, latency, and cost.

## Run

```bash
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```
